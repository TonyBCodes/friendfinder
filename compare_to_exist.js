var existing = require("./app/data/friends");
var new;

var diff = [{
    var index,
    var score
}]

for (outer = 0; outer < existing.length; outer++) {

    for (inner = 0; inner < existing[outer].scores.length; inner++)
}